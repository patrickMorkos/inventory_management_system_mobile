
import 'package:flutter/material.dart';

class SalesmansContent extends StatelessWidget {
  const SalesmansContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Center(
          child: Text("Salesmans Content"),
        ),
      ),
    );
  }
}
