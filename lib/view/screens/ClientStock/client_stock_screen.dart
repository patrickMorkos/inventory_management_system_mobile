import 'package:flutter/material.dart';

class ClientStockScreen extends StatelessWidget {
  const ClientStockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("ClientStockScreen"),
      ),
    );
  }
}
