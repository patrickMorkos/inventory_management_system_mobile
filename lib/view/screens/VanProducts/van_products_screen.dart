import 'package:flutter/material.dart';

class VanProductsScreen extends StatelessWidget {
  const VanProductsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("VanProductsScreen"),
      ),
    );
  }
}
