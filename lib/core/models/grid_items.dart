class GridItems {
  final String title, icon, route;
  final bool showCard;

  GridItems({
    required this.route,
    required this.title,
    required this.icon,
    required this.showCard,
  });
}
